# Nombre de la aplicacion
spring.application.name=backend

# Configuracion del puerto del servidor
server.port=8080

#Configuracion de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/moodflix_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=enzo
spring.datasource.password=EnzoPaoloMazzariol

# Configuracion del driver de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Validacionn de Conexion
spring.datasource.hikari.validation-timeout=4000
spring.datasource.hikari.connection-test-query=SELECT 1

